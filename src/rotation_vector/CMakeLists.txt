IF("${RV}" STREQUAL "ON")
add_subdirectory(rv)
ENDIF()
IF("${RV_RAW}" STREQUAL "ON")
	add_subdirectory(rv_raw)
ENDIF()
IF("${GEOMAGNETIC_RV}" STREQUAL "ON")
add_subdirectory(geomagnetic_rv)
ENDIF()
IF("${GAMING_RV}" STREQUAL "ON")
add_subdirectory(gaming_rv)
ENDIF()